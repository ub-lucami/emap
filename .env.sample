# Example environment file (DO NOT commit real secrets)
# Copy this to `.env.local` or use as a template in Portainer when creating the stack.

# External web port (host)
WEB_PORT=8001

# Database
DB_ROOT_PASSWORD=changeme_root_password
DB_NAME=emapp
DB_USER=emapp_user
DB_PASSWORD=changeme_db_password

# Django
SECRET_KEY=replace-with-a-long-random-secret
# Comma-separated list of allowed hosts (no spaces)
ALLOWED_HOSTS=localhost,127.0.0.1,docker-alpine.lucami.org,192.168.81.21
DEBUG=False
DB_ENGINE=django.db.backends.mysql
LANGUAGE_CODE=en
TIME_ZONE=Europe/Ljubljana

# CSRF trusted origins (include scheme and port if nonstandard)
# e.g. CSRF_TRUSTED_ORIGINS=http://192.168.81.21:8001,http://docker-alpine.lucami.org:8001
CSRF_TRUSTED_ORIGINS=
